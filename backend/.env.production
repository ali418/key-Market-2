# Environment Variables for Backend Production

# Node Environment
NODE_ENV=production

# Server Configuration
PORT=3001
API_PREFIX=/api/v1

# Database Configuration - Railway will provide these via DATABASE_URL
# These are fallback values, Railway's DATABASE_URL will override them
DB_HOST=${DB_HOST}
DB_PORT=${DB_PORT}
DB_NAME=${DB_NAME}
DB_USER=${DB_USER}
DB_PASSWORD=${DB_PASSWORD}

# Railway PostgreSQL URL (this will be automatically provided by Railway)
DATABASE_URL=${DATABASE_URL}

# JWT Configuration
JWT_SECRET=${qQgD4v!w_9aXcJD}
JWT_EXPIRES_IN=12h
JWT_REFRESH_SECRET=${ZzuF4MN612W6xcoGmZblGg54Ebv}
JWT_REFRESH_EXPIRES_IN=7d

# CORS Configuration
CORS_ORIGIN=https://key-market.up.railway.app,https://key-market-production.up.railway.app,https://keymarket.com

# Logging Configuration
LOG_LEVEL=info

# File Upload Configuration
UPLOAD_DIR=uploads
MAX_FILE_SIZE=5242880 # 5MB

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000 # 15 minutes
RATE_LIMIT_MAX=100 # 100 requests per window

# Security
HELMET_ENABLED=true
COMPRESSION_ENABLED=true