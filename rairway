نظام يتكون من:

- Frontend : تطبيق React (في مجلد frontend )
- Backend : خادم Node.js/Express (في مجلد backend )
- Database : قاعدة بيانات PostgreSQL
- File Uploads : نظام رفع الملفات (مجلد uploads )
## 📋 الخطوات المطلوبة لرفع نظام مشابه على Railway:
### 1. إعداد ملفات المشروع الأساسية: أ) ملف package.json الرئيسي:
```
{
  "name": "your-project-name",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm 
    install && npm run build && 
    cd ../backend && npm install",
    "start": "cd backend && npm 
    start"
  }
}
``` ب) ملف Procfile :
```
web: cd backend && npm start
``` ج) ملف railway.json :
```
{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build"
  },
  "deploy": {
    "startCommand": "cd backend && 
    npm start",
    "healthcheckPath": "/api/
    health",
    "healthcheckTimeout": 60,
    "restartPolicyType": 
    "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
```

2/5 done

شرح متطلبات Railway وإعداد المشروع

توضيح ملفات الإعداد المطلوبة (Procfile, package.json, etc)

شرح إعداد قاعدة البيانات على Railway

مراجعة بنية النظام الحالي وفهم مكوناته

شرح متطلبات Railway وإعداد المشروع

توضيح ملفات الإعداد المطلوبة (Procfile, package.json, etc)

شرح إعداد قاعدة البيانات على Railway

توضيح كيفية رفع المشروع ونشره

5/5 done

All tasks completed

مراجعة بنية النظام الحالي وفهم مكوناته

شرح متطلبات Railway وإعداد المشروع

توضيح ملفات الإعداد المطلوبة (Procfile, package.json, etc)

شرح إعداد قاعدة البيانات على Railway

توضيح كيفية رفع المشروع ونشره

بعد مراجعة النظام بالتفصيل، هاي الخطوات الكاملة اللي تحتاجها عشان ترفع نظام مشابه على Railway:

## 📋 بنية النظام الحالي
النظام ده مكون من:

- Frontend : React.js application (في مجلد frontend/ )
- Backend : Node.js/Express API (في مجلد backend/ )
- Database : PostgreSQL مع Sequelize ORM
- File Uploads : نظام رفع الملفات في مجلد uploads/
## 🚀 الخطوات المطلوبة لرفع النظام على Railway
### 1. إعداد الملفات الأساسية أ) ملف package.json الرئيسي:
```
{
  "name": "cafesundus",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm 
    install && npm run build && 
    cd ../backend && npm install",
    "start": "cd backend && npm 
    start"
  }
}
``` ب) ملف Procfile :
```
web: cd backend && npm start
``` ج) ملف railway.json :
```
{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build"
  },
  "deploy": {
    "startCommand": "cd backend && 
    npm start",
    "healthcheckPath": "/api/
    health",
    "healthcheckTimeout": 60,
    "restartPolicyType": 
    "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
```
### 2. إعداد Backend أ) ملف backend/package.json يجب يحتوي على:
```
{
  "scripts": {
    "start": "node src/index.js",
    "postinstall": "npm install 
    --prefix ../frontend && npm run 
    build --prefix ../frontend"
  },
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.16.3",
    "sequelize": "^6.31.1",
    // باقي التبعيات...
  }
}
``` ب) ملف backend/src/index.js يجب يحتوي على:
```
const express = require('express');
const path = require('path');
const app = express();
const PORT = process.env.PORT || 
3000;

// إعداد الملفات الثابتة
app.use(express.static(path.join
(__dirname, '../../frontend/
build')));
app.use('/uploads', express.static
(path.join(__dirname, '..', process.
env.UPLOAD_DIR || 'uploads')));

// API routes
// ... باقي الـ routes

// Catch-all handler
app.get('*', (req, res) => {
  res.sendFile(path.join(__dirname, 
  '../../frontend/build', 'index.
  html'));
});

app.listen(PORT, () => {
  console.log(`Server running on 
  port ${PORT}`);
});
```
### 3. إعداد قاعدة البيانات أ) إنشاء PostgreSQL Database على Railway:
1. 1.
   اذهب إلى Railway Dashboard
2. 2.
   اضغط على "New Project"
3. 3.
   اختر "Add PostgreSQL"
4. 4.
   احفظ الـ connection details ب) إعداد متغيرات البيئة:
```
DATABASE_URL=postgresql://
username:password@host:port/database
NODE_ENV=production
PORT=3000
JWT_SECRET=your-jwt-secret
UPLOAD_DIR=uploads
```
### 4. إعداد Frontend أ) ملف frontend/package.json :
```
{
  "scripts": {
    "build": "react-scripts build",
    "start": "cross-env PORT=3001 
    react-scripts start"
  },
  "homepage": "."
}
``` ب) إعداد API Base URL في Frontend:
```
// في ملف config أو api service
const API_BASE_URL = process.env.
NODE_ENV === 'production' 
  ? '' // استخدم نفس الدومين
  : 'http://localhost:3000';
```
### 5. خطوات الرفع على Railway أ) إعداد Git Repository:
```
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://
github.com/yourusername/your-repo.
git
git push -u origin main
``` ب) ربط المشروع بـ Railway:
1. 1.
   اذهب إلى railway.app
2. 2.
   اضغط على "New Project"
3. 3.
   اختر "Deploy from GitHub repo"
4. 4.
   اختر الـ repository الخاص بك
5. 5.
   Railway هيكتشف الإعدادات تلقائياً من railway.json ج) إعداد متغيرات البيئة:
1. 1.
   في Railway Dashboard، اذهب لـ Variables
2. 2.
   أضف المتغيرات المطلوبة:
   - DATABASE_URL
   - NODE_ENV=production
   - JWT_SECRET
   - UPLOAD_DIR=uploads د) إعداد الدومين:
1. 1.
   في Railway Dashboard، اذهب لـ Settings
2. 2.
   في قسم Domains، اضغط "Generate Domain"
3. 3.
   أو أضف Custom Domain إذا كان عندك
### 6. نصائح مهمة أ) إدارة الملفات المرفوعة:
```
// استخدم Railway Volume أو 
External Storage
const uploadPath = process.env.
NODE_ENV === 'production' 
  ? '/app/uploads' 
  : path.join(__dirname, 'uploads');
``` ب) إعداد CORS:
```
app.use(cors({
  origin: process.env.NODE_ENV === 
  'production' 
    ? ['https://yourdomain.railway.
    app'] 
    : ['http://localhost:3001'],
  credentials: true
}));
``` ج) Health Check Endpoint:
```
app.get('/api/health', (req, res) 
=> {
  res.json({ 
    status: 'ok', 
    timestamp: new Date().
    toISOString(),
    environment: process.env.
    NODE_ENV 
  });
});
```
### 7. مراقبة ومتابعة أ) Logs:
- استخدم Railway Dashboard لمراقبة الـ logs
- أضف logging مناسب في الكود ب) Database Migrations:
```
// في startup script
if (process.env.NODE_ENV === 
'production') {
  await sequelize.sync({ alter: 
  true });
}
```
### 8. الملفات المطلوبة في المشروع الجديد
تأكد إن عندك الملفات دي:

- ✅ package.json (root)
- ✅ Procfile
- ✅ railway.json
- ✅ backend/package.json
- ✅ frontend/package.json
- ✅ .gitignore
- ✅ README.md
### 9. أخطاء شائعة وحلولها أ) مشكلة Build:
```
// في backend/package.json
"postinstall": "npm install 
--prefix ../frontend && npm run 
build --prefix ../frontend"
``` ب) مشكلة Static Files:
```
// تأكد من المسار الصحيح
app.use(express.static(path.join
(__dirname, '../../frontend/
build')));
``` ج) مشكلة Database Connection:
```
// استخدم DATABASE_URL مباشرة
const sequelize = new Sequelize
(process.env.DATABASE_URL, {
  dialect: 'postgres',
  dialectOptions: {
    ssl: process.env.NODE_ENV === 
    'production' ? {
      require: true,
      rejectUnauthorized: false
    } : false
  }
});
```
هاي كل الخطوات المطلوبة بالتفصيل. إذا عندك نظام تاني وعايز ترفعه، اتبع نفس الخطوات دي وغير الأسماء والإعدادات حسب